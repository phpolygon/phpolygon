{
    "name": "phpolygon/phpolygon",
    "description": "PHP-native game engine with AI-first authoring",
    "type": "library",
    "license": "MIT",
    "require": {
        "php": ">=8.5",
        "ext-vio": "*"
    },
    "suggest": {
        "ext-glfw": "OpenGL backend (legacy) - required when using Renderer2D/OpenGLRenderer3D/Window/Input",
        "ext-vulkan": "Vulkan 3D backend (renderBackend3D: 'vulkan') - install via PIE: pie install hmennen90/php-vulkan",
        "ext-metal": "Metal 3D backend, macOS only (renderBackend3D: 'metal') - install via PIE: pie install hmennen90/php-metal-gpu:@dev",
        "ext-gd": "Required for visual regression testing (GdRenderer2D) and logo/image generation",
        "ext-ffi": "Alternative GPU binding mechanism (not used for frame-critical calls)",
        "ext-parallel": "Required for multithreading support (ZTS build)",
        "ext-spx": "Dev-only deep-dive profiler with flamegraph UI - install via scripts/install-profilers.sh",
        "ext-excimer": "Dev-only sampling profiler for CI regression checks - install via scripts/install-profilers.sh"
    },
    "require-dev": {
        "phpunit/phpunit": "^11.0",
        "phpstan/phpstan": "^2.0",
        "phpbench/phpbench": "^1.3"
    },
    "autoload": {
        "psr-4": {
            "PHPolygon\\": "src/"
        }
    },
    "autoload-dev": {
        "psr-4": {
            "PHPolygon\\Tests\\": "tests/"
        }
    },
    "bin": ["bin/phpolygon"],
    "minimum-stability": "stable",
    "prefer-stable": true
}
